# update symlink to current SSH_AUTH_SOCK if it doesn't already exist
if [[ ! -S "${HOME}/.ssh/ssh_auth_sock" ]] && [[ -S "${SSH_AUTH_SOCK}" ]]; then
  ln -sf $SSH_AUTH_SOCK "${HOME}/.ssh/ssh_auth_sock"
fi
